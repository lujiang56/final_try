# 6.5 开发日志 — 期末突击备考 Web 应用

## 概述

本轮完成 Debug 调试聊天页面、PPT 分析架构简化、前端重写，以及一场耗时 2 小时的 HTML 标签 Bug 排查。

---

## 一、新增功能

### 1.1 LLM Debug 调试控制台

**路由**：`/debug`、`/api/debug/chat`

直接与 LLM API 交互的开发者工具：
- 支持 Anthropic / OpenAI / Custom 三种 Provider
- 流式 SSE 输出，逐 token 显示
- 可编辑 System Prompt、Temperature、Max Tokens
- 多轮对话历史在内存中保持
- 左侧面板显示当前连接信息（Provider、Model、Base URL）
- 页面顶部导航栏新增 **Debug** 入口

### 1.2 PPT 课件对话页面

**路由**：`/ppt-analysis`、`/ppt-analysis/<session_key>`

完全重写的 PPT 上传与对话界面：
- **2 步流程**：上传课件 → AI 对话
- **XHR 上传**：实时进度条（百分比 + 已传输/总大小），可取消
- **上传即聊天**：PPT 全文嵌入 system prompt，LLM 基于原文回答
- **流式聊天**：SSE 逐 token 渲染，Bootstrap 气泡样式
- **文件状态卡片**：显示文件名、页数、状态（已就绪/分析完成/已取消）
- **复习资料生成**：基于 PPT 全文 + LLM，一键生成总结/速查表/突破指南

---

## 二、架构变更

### 2.1 删除词义分析模块

| 删除文件 | 原因 |
|---|---|
| `ppt_analyzer.py` | 规则引擎（关键词匹配），已零引用 |
| `ppt_llm_analyzer.py` | 结构化 LLM 分析（章节→考点→分值），流程已简化 |
| `test_ppt.py` / `test/test_ppt_v2.py` / `test/test_ppt_analysis.py` / `test/create_test_ppt.py` | 引用已删除模块的测试文件 |
| `test/sample_*.pptx` | 测试课件 |

### 2.2 简化前后对比

```
旧流程：上传 PPT → 提取幻灯片 → LLM 结构化分析（章节/考点/分值）
       → 解析 JSON → 写入 knowledge_point 表 → 显示分析侧栏
       → 用户聊天（基于分析摘要）

新流程：上传 PPT → 提取幻灯片 → 创建会话(status=ready)
       → PPT 全文嵌入 system prompt → 用户直接聊天
```

### 2.3 `ppt_analysis.html` 独立化

从 `{% extends "base.html" %}` 改为独立 HTML 文档，原因：
- `base.html` 加载的 `app.js` 含有全局表单处理器，会干扰 XHR 上传流程
- `app.js` 的上传区域事件处理器与页面内 JS 重复绑定
- 独立后页面完全自控，不受其他页面逻辑影响

---

## 三、Bug 修复清单

### 3.1 Flask SSE Context 丢失（严重）

**现象**：Debug 聊天和 PPT 分析的 SSE 端点返回"未配置 LLM API Key"，但设置页显示已配置。

**根因**：SSE 生成器在 Flask 返回 `Response` 后才执行，此时 `current_app`（Flask application context）已销毁。`llm_config.py` 的 `_get_encryption_key()` 在 context 丢失后回退到基于机器名的 fallback key，与加密时使用的 key 不同，解密失败返回空字符串。`decrypt_api_key()` 的 `except Exception: return ''` 静默吞掉了异常。

**修复**：
- `llm_config.py`：添加 `_encryption_key_cache` 全局缓存，首次在 Flask context 内计算后固定
- `app.py`：所有 SSE 端点在请求处理器内（context 存活时）调用 `_get_encryption_key()` 预热缓存 + 调用 `_get_config()` 获取配置，通过闭包传入生成器
- `llm_client.py`：`call_llm_stream` / `call_llm_stream_raw` 新增 `config` 可选参数

**影响范围**：`/api/debug/chat`、`/api/ppt-analysis/<key>/analyze`、`/api/ppt-analysis/<key>/chat`

### 3.2 XHR 回调检查顺序错误

**现象**：上传 PPTX 后"毫无反应"——无进度条、无状态变化。

**根因**：`uploadXHR.addEventListener('load', ...)` 回调中，`uploadXHR = null` 写在 `if (uploadXHR === null) return` **之前**，导致每次上传成功都立即被当作"已取消"跳过，`startAnalysis()` 从未被调用。

**修复**：先检查 `uploadXHR === null`，再置 null。

### 3.3 app.js 表单处理器冲突

**现象**：`app.js` 的全局表单处理器会捕获所有 `<form>` 的 submit 事件，将提交按钮禁用 2 秒后重新启用，破坏了 XHR 上传按钮的状态管理。

**修复**：`app.js` 跳过 `id="upload-form"` 的表单；上传区域处理器仅对 `materials.html`（`id="ppt-upload-form"`）生效。

### 3.4 HTML 未闭合标签导致 DOM 解析异常（本次最隐蔽的 Bug）

**现象**：`document.querySelector('#upload-form')` 在 DOMContentLoaded 回调中返回 `null`，但 form 标签**确实存在于服务端返回的 HTML 中**。内联脚本在 form 标签后立即执行时能找到，但 DOMContentLoaded 时找不到。

**排查过程**：
1. 后端 API 测试通过 → 排除后端
2. 添加 `Cache-Control: no-cache` → 问题依旧 → 排除浏览器缓存
3. 去掉 `app.js` 加载 → 问题依旧 → 排除 JS 干扰
4. 创建独立最小化测试页面 `test_upload.html` → 正常 → 确认问题在模板自身
5. 在 form 标签后插入内联检查脚本 → `getElementById` 返回 true，但 DOMContentLoaded 中 `querySelector` 返回 null
6. 统计 `<div>` 数量：46 个开标签 vs 45 个闭标签 → **发现少一个 `</div>`**

**根因**：将模板从 `{% extends "base.html" %}` 改为独立 HTML 时，`<div class="container mt-4 mb-5">` 缺少对应的 `</div>` 关闭标签。浏览器解析器自动修复未闭合标签时重构了 DOM 树，导致 `querySelector` 在不同时机返回不一致的结果。

**修复**：补上缺失的 `</div>`；添加防御代码——内联保存 form 引用到 `window.__UPLOAD_FORM_REF`，DOMContentLoaded 中若 `querySelector` 失败则回退使用。

**教训**：HTML 结构完整性是第一道防线。一个未闭合标签可以让所有 DOM 查询静默失效，且错误信息完全不指向根因。`getElementById` 在 DOM 修复后比 `querySelector` 更稳定。

### 3.5 依赖缺失

| 包 | 修复 |
|---|---|
| `cryptography` | `pip install cryptography` |
| `openai` | `pip install openai`（DeepSeek 使用 OpenAI 兼容 API） |

---

## 四、文件变更摘要

| 文件 | 变更类型 |
|---|---|
| `app.py` | 大量修改 — 新增 Debug 路由、删除分析端点、简化为上传即聊天、SSE context 修复 |
| `llm_config.py` | 修改 — 加密密钥缓存 |
| `llm_client.py` | 修改 — 流式函数支持 `config` 参数 |
| `database.py` | 修改 — `create_analysis_session` 支持 `status` 参数 |
| `static/app.js` | 修改 — 跳过 PPT 页面的表单，仅对 materials 页面生效 |
| `static/style.css` | 修改 — 新步骤条样式、上传区视觉升级 |
| `templates/ppt_analysis.html` | 完全重写 — 独立页面、2 步流程、XHR 上传、流式聊天 |
| `templates/debug_chat.html` | 新建 — LLM Debug 控制台 |
| `templates/base.html` | 修改 — 导航栏新增 Debug 入口 |
| `ppt_analyzer.py` | **删除** |
| `ppt_llm_analyzer.py` | **删除** |
| `test_ppt.py` | **删除** |
| `test/test_ppt_v2.py` | **删除** |
| `test/test_ppt_analysis.py` | **删除** |
| `test/create_test_ppt.py` | **删除** |
| `test/sample_*.pptx` | **删除** |
| `DEBUG_REPORT.md` | 新建 — 本次调试追溯报告 |

---

## 五、项目当前架构

```
final_try/
├── app.py                  # Flask 主入口（路由+业务逻辑，约1000行）
├── database.py             # SQLite CRUD
├── llm_client.py           # LLM API 封装（Anthropic/OpenAI/Custom）
├── llm_config.py           # API Key AES-256-GCM 加密存储
├── ppt_handler.py          # PPTX 文本/幻灯片提取
├── analyzer.py             # 文本材料考点分析（materials 页面用）
├── material_generator.py   # 复习资料生成
├── planner.py              # 突击计划生成
├── static/
│   ├── app.js              # 全局 JS（仅 materials 页面生效）
│   └── style.css           # 全局样式
├── templates/
│   ├── ppt_analysis.html   # PPT 课件对话（独立页面）
│   ├── debug_chat.html     # LLM Debug 控制台
│   ├── base.html           # 基础模板（其他页面）
│   ├── dashboard.html      # 仪表盘
│   ├── index.html          # 首页
│   ├── subject.html        # 单科详情
│   ├── materials.html      # 材料分析
│   ├── plan.html           # 突击计划
│   ├── library.html        # 资料库
│   ├── material_view.html  # 资料查看
│   └── settings.html       # LLM 设置
├── uploads/                # PPT 临时上传目录
├── DEBUG_REPORT.md         # 调试追溯报告
└── 6.5.md                  # 本日志
```

---

## 六、后续建议

1. **清理 `uploads/` 目录** — 旧文件 `chapter1.pptx` 残留，服务器不会自动清理
2. **`app.py` 拆分 Blueprint** — 当前 1000+ 行，建议按模块拆分
3. **`decrypt_api_key()` 不要静默吞异常** — 至少 `print()` 或 `logging.warning()`
4. **向量库** — 用户提出需求，用于长课件的分片检索。建议 `chromadb` 或 `sqlite-vec`
5. **HTML validator 加入 CI** — 避免再被未闭合标签坑 2 小时
6. **其他页面也独立化** — 避免 `app.js` 全局处理器干扰

---

*日志生成于 2026-06-05，记录本轮全部开发活动。*
